fn f32 @Test(f32 %a, f32 %b) {
    return div(%a, %b);
}

fn u32 @Main(u32 %num) {
    u32 %i = u32(0);

    loop:
    %j = add(%j, %i);

    %i = add(%i, u32(1));
    jumpc loop, lt(%i, u32(10));

    return %j;
}